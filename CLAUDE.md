# CLAUDE.md

このファイルは、このリポジトリで Claude Code が作業する際のガイドです。

## プロジェクト概要

**宝石BoxHunter** — 宝箱をクリックして開け、宝石を集めるブラウザゲーム。
Vanilla HTML / CSS / JavaScript のみで実装（ビルドツール・フレームワーク不使用）。
`index.html` をブラウザで直接開けば動作する（`file://` でも可、サーバー不要）。

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | ログイン／新規登録画面＋ゲーム本体（6×6の宝箱盤・結果テーブル） |
| `ranking.html` | 全ユーザー中TOP30の合計点数ランキング画面 |
| `style.css` | 全ページ共通スタイル |
| `app.js` | 共通ロジック：ユーザー登録／ログイン、localStorage永続化ヘルパー |
| `game.js` | `index.html` 専用：宝箱の抽選・開閉アニメーション・結果記録 |
| `ranking.js` | `ranking.html` 専用：TOP30集計・表示 |
| `img/` | 画像アセット（後述） |

ES Modules は使わず `<script>` を順番読み込みするだけの構成（`app.js` → `game.js`/`ranking.js`）。
`file://` で開いても CORS エラーが出ないようにするための意図的な選択なので、モジュール化しない。

## ゲームルール

- 宝箱は全36個、6×6グリッドで配置。
- 箱をクリックすると開封アニメーション（`popFront` キーフレーム：拡大しながら前面にせり出す）とともに中身を1回抽選する。
- 抽選確率と点数（合計重み36で正規化。ユーザー原文の分母36/36/36/25/24は36個という前提と矛盾していたため、36に揃えて解釈し直したもの）：

  | 宝石 | 確率 | 点数 | 画像 |
  |---|---|---|---|
  | ダイヤモンド | 1/36 | 1000 | `img/BOX_Op_Dia.png` |
  | ルビー | 3/36 | 100 | `img/BOX_Op_Rub.png` |
  | サファイア | 6/36 | 10 | `img/BOX_Op_Saf.png` |
  | トパーズ | 9/36 | 1 | `img/BOX_Op_To.png` |
  | 空 | 17/36 | 0 | `img/BOX_Op.png` |

- 開封時に `boxopen.mp3` を再生する。
- 開封表示は約4秒間（`game.js` の `OPEN_DISPLAY_MS`）。その間は他の箱の再クリックは不可（見た目は不透明化せず通常表示のまま無効化）。
- 4秒経過後は、その箱だけを閉じるのではなく **盤面36個すべてを一度消去してから再構築**（`buildBoard()` の再実行）して全closed状態に戻す。これが仕様上の「都度全部締まる／全体を消去して再表示する」の実装。
- 抽選結果は結果テーブルの先頭（最新が上）に追加され、合計点数（テーブル上部に表示）にも即時反映される。

`game.js` の `PRIZES` 配列が確率テーブルの唯一の情報源。確率や点数を変更する場合はここだけを編集する。

## 画像・音声アセット

画像は `img/` フォルダに格納。

- `BOX.png` … 閉じた宝箱
- `BOX_Op.png` … 開いた宝箱（空の場合にも使用）
- `BOX_Op_Dia.png` … ダイヤモンド入り
- `BOX_Op_Rub.png` … ルビー入り
- `BOX_Op_Saf.png` … サファイア入り
- `BOX_Op_To.png` … トパーズ入り

音声はルート直下に配置。開封結果に応じて `game.js` の `PRIZES[].sound` が再生される。

- `boxopen.mp3` … 空（ハズレ）の場合の開封音
- `Dia.mp3` … ダイヤモンド
- `Rub.mp3` … ルビー
- `Saf.mp3` … サファイア
- `To.mp3` … トパーズ

## データ永続化（現状の割り切り）

DB未接続のため `localStorage` で代替している。将来的にサーバーAPI／DBへ差し替える前提の暫定実装であり、恒久設計として扱わない。

- `bh_users` … `{ username: passwordHashHex }`（SHA-256、`crypto.subtle` で生成）
- `bh_session` … ログイン中のユーザー名
- `bh_totals` … `{ username: 合計点数 }`（ランキング画面はこれをソートしてTOP30表示）
- `bh_history_<username>` … そのユーザーの開封履歴配列（新しい記録が先頭）

DB連携に着手する際は `app.js` の `getUsers`/`saveUsers`/`getTotals`/`saveTotals`/`getHistory`/`saveHistory`/`addRecord` をAPI呼び出しに置き換える想定。呼び出し側（`game.js`/`ranking.js`）のインターフェースは変えずに済むよう設計してある。

## デザイン方針

- 背景色：薄茶色（`--bg-color: #e8d6b8`）
- フォント：明朝体（`游明朝` / `Yu Mincho` / `MS PMincho` 等のフォールバック）
- 見出し：太字・36px（`h1.title`）
- 宝箱盤：石畳風の背景（CSSグラデーションで表現、画像アセット不使用）＋縁取り枠
- フッター：`© 2026 KT Corporation`
- レスポンシブ：`style.css` 末尾のメディアクエリで、タブレット（〜1024px）・SP（〜600px）では `body`余白／`.board-wrap`余白・枠線／`.board`のgapを詰め、宝箱1個の見た目サイズをデスクトップ相対で約1.2〜1.4倍に拡大（6×6配置は変更しない）。数値をさらに詰めれば拡大率は上げられるが、詰めすぎると石畳の縁取りが失われるため、枠が視認できる範囲で調整済み。

## 今後の想定タスク（未着手）

- localStorage → 実データベース（サーバーAPI）への移行
- ユーザー登録のセキュリティ強化（現状はクライアントサイドSHA-256のみ、実運用にはサーバーサイド認証が必須）

## GitHubリポジトリ

https://github.com/FALCONKT/BoxApp.git
